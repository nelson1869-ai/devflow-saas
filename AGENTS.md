<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# Project Context

- The runnable Next.js project and independent Git root is `/home/nelson/0/nelson-tutorial/Next.js-15-Tutorials/a-learning`.
- `src/app/devflow-saas/` contains DevFlow SaaS, the production-style application Nelson is building.
- Before working in DevFlow, read and follow `src/app/devflow-saas/AGENTS.md`.
- `src/app/learn/` is a separate local reference curriculum for learning TypeScript, React, UI/UX, and Next.js server/client concepts.
- `src/app/learn/` is intentionally ignored by this repository. Do not stage, commit, or modify it while working on DevFlow unless Nelson explicitly requests learning-curriculum work.
- Files outside `a-learning` may be inspected read-only when useful, but must not be modified as part of DevFlow work.
- Nelson writes and edits all DevFlow application source code. Codex acts only as mentor and reviewer.
- Give Nelson one small implementation step at a time, stop, and wait for `done`.
- When Nelson replies `done`, inspect and verify the work before approving it or requesting the smallest necessary correction.
- Do not install dependencies, edit application source, or create Git commits for Nelson.
